import 'package:flutter/material.dart';

class AppColors {
  // Primary (Teal Palette)
  static const Color primary = Color(0xFF14B8A6); // Teal 500
  static const Color primaryHover = Color(0xFF0D9488); // Teal 600
  static const Color primarySoftLight = Color(0xFFCCFBF1); // Teal 100
  static const Color primarySoftDark = Color(0xFF042F2E); // Teal 950

  // Secondary
  static const Color secondaryLight = Color(0xFFF0FDFA);
  static const Color secondaryDark = Color(0xFF143636);
  static const Color secondaryForegroundLight = Color(0xFF0F172A);
  static const Color secondaryForegroundDark = Color(0xFFECFEFF);

  // Background
  static const Color backgroundLight = Color(0xFFF8FAFC);
  static const Color backgroundDark = Color(0xFF071A1A);

  // Card / Surface
  static const Color cardLight = Color(0xFFFFFFFF);
  static const Color cardDark = Color(0xFF0F2A2A);

  // Text / Foreground
  static const Color foregroundLight = Color(0xFF0F172A);
  static const Color foregroundDark = Color(0xFFECFEFF);

  // Muted
  static const Color mutedLight = Color(0xFFF0FDFA);
  static const Color mutedDark = Color(0xFF143636);

  static const Color mutedForegroundLight = Color(0xFF64748B);
  static const Color mutedForegroundDark = Color(0xFF7FA3A3);

  // Border & Input
  static const Color borderLight = Color(0xFFE2E8F0);
  static const Color borderDark = Color(0xFF245252);

  static const Color inputLight = Color(0xFFE2E8F0);
  static const Color inputDark = Color(0xFF245252);

  // Accent & Status Colors
  static const Color accent = Color(0xFFF97316); // Orange 500
  static const Color danger = Color(0xFFF43F5E); // Rose 500
  static const Color success = Color(0xFF22C55E); // Green 500
  static const Color warning = Color(0xFFF59E0B); // Amber 500
  static const Color info = Color(0xFF38BDF8); // Sky 400
  static const Color ring = Color(0xFF14B8A6);
}
